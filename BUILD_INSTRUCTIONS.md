# تعليمات بناء تطبيق Windows - نظام جدولة الحصص المدرسية

## نظرة عامة
هذا المشروع الآن جاهز ليعمل كتطبيق Windows مستقل (EXE) يعمل **بدون إنترنت** مع قاعدة بيانات محلية SQLite.

## ✅ التغييرات المهمة التي تمت
1. ✅ تحويل التخزين من MemStorage إلى SQLite (قاعدة بيانات محلية)
2. ✅ إضافة Electron لتحويل التطبيق إلى تطبيق Windows
3. ✅ إعداد كامل لبناء ملف EXE
4. ✅ جميع الوظائف تعمل بشكل كامل (المعلمين، الجداول، Excel، PDF)

## 📦 المتطلبات

### 1. تثبيت Node.js
- قم بتحميل وتثبيت Node.js من: https://nodejs.org/
- النسخة الموصى بها: **v18 أو أحدث**
- تأكد من تفعيل خيار "Add to PATH" أثناء التثبيت

### 2. التحقق من التثبيت
افتح Command Prompt (CMD) وقم بتشغيل:
```bash
node --version
npm --version
```
يجب أن ترى رقم الإصدار لكليهما.

## 🚀 خطوات بناء التطبيق

### الخطوة 1: تحميل المشروع
1. حمل المشروع من Replit كملف ZIP
2. استخرج الملفات إلى مجلد على جهازك (مثلاً: `C:\school-schedule`)

### الخطوة 2: تثبيت المكتبات
افتح Command Prompt في مجلد المشروع وقم بتشغيل:
```bash
npm install
```
⏳ هذه الخطوة قد تستغرق 5-10 دقائق لأول مرة.

### الخطوة 3: تثبيت Electron و Electron Builder
```bash
npm install --save-dev electron electron-builder
```

### الخطوة 4: بناء ملف EXE
```bash
npm run electron:build
```

⏳ **الانتظار**: هذه الخطوة تستغرق 5-15 دقيقة حسب سرعة جهازك.

### الخطوة 5: العثور على ملف EXE
بعد اكتمال البناء، ستجد ملف التطبيق في:
```
build\
  └── نظام جدولة الحصص المدرسية-1.0.0-x64.exe  (installer)
```

## 📱 تشغيل التطبيق

### الطريقة الأولى: Installer (موصى بها)
1. شغّل ملف `نظام جدولة الحصص المدرسية-1.0.0-x64.exe`
2. اتبع خطوات التثبيت
3. بعد التثبيت، ستجد اختصار على سطح المكتب
4. شغل التطبيق من الاختصار

### الطريقة الثانية: التشغيل المباشر من المشروع (للتطوير)
```bash
npm run electron:dev
```

## 💾 حفظ البيانات

### أين تُحفظ البيانات؟
- **أثناء التطوير**: في ملف `school-schedule.db` داخل مجلد المشروع
- **بعد التثبيت**: في مجلد بيانات المستخدم:
  ```
  C:\Users\{اسم المستخدم}\AppData\Roaming\school-schedule-app\school-schedule.db
  ```

### ملاحظات مهمة عن البيانات
- ✅ البيانات محفوظة محلياً في ملف واحد
- ✅ لا تحتاج إنترنت للعمل
- ✅ البيانات لا تُحذف عند إغلاق التطبيق
- ✅ يمكنك نسخ ملف `.db` كنسخة احتياطية

## 🔧 حل المشاكل

### مشكلة: "npm: command not found"
**الحل**: تأكد من تثبيت Node.js بشكل صحيح وأضف PATH

### مشكلة: "Cannot find module 'electron'"
**الحل**: قم بتشغيل:
```bash
npm install --save-dev electron electron-builder
```

### مشكلة: البناء يفشل بسبب "Python required"
**الحل**: 
1. قم بتثبيت Python من: https://www.python.org/downloads/
2. تأكد من تفعيل "Add Python to PATH"
3. أعد تشغيل الـ CMD
4. حاول مرة أخرى

### مشكلة: التطبيق يفتح صفحة فارغة
**الحل**: 
1. تأكد من اكتمال عملية البناء بنجاح
2. تحقق من وجود مجلد `dist` داخل المشروع
3. أعد بناء المشروع:
```bash
npm run build
npm run electron:build
```

### مشكلة: الوظائف لا تعمل (الأزرار لا تستجيب)
**السبب المحتمل**: لم يتم بناء المشروع بشكل صحيح
**الحل**:
1. احذف مجلد `node_modules` و `dist`
2. قم بتشغيل:
```bash
npm install
npm run build
npm run electron:build
```

## 📝 ملاحظات إضافية

### هيكل المشروع
```
school-schedule/
├── electron-main.js          # ملف Electron الرئيسي
├── electron-preload.js       # جسر الأمان
├── electron-builder.yml      # إعدادات البناء
├── server/
│   ├── sqlite-storage.ts     # قاعدة بيانات SQLite
│   ├── storage.ts           # واجهة التخزين
│   ├── index.ts             # Express server
│   └── routes.ts            # API routes
├── client/                   # Frontend (React)
├── dist/                     # ملفات البناء
└── build/                    # التطبيق النهائي
```

### الميزات المدعومة بالكامل
- ✅ إدارة المعلمين (إضافة، تعديل، حذف)
- ✅ جدولة الحصص لكل معلم
- ✅ الجدول الرئيسي لجميع المعلمين
- ✅ جداول الصفوف
- ✅ استيراد من Excel
- ✅ تصدير إلى Excel و PDF
- ✅ إدارة الشعب الدراسية
- ✅ كشف التعارضات
- ✅ حذف الحصص الفردية
- ✅ دمج الحصص المتتالية في التصدير
- ✅ واجهة عربية كاملة مع RTL

## 🎉 نجاح!
بعد اتباع هذه الخطوات، سيكون لديك تطبيق Windows كامل يعمل بدون إنترنت مع جميع الميزات!

### للدعم
إذا واجهت أي مشكلة:
1. تأكد من اتباع جميع الخطوات بالترتيب
2. تحقق من رسائل الخطأ في CMD
3. تأكد من تثبيت جميع المتطلبات (Node.js, Python)

---
**تم التحويل بنجاح من Web App إلى Desktop App** ✅
